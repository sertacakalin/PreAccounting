spring:
  datasource:
    url: jdbc:mysql://mysql:3306/pre_accounting_db?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: pokok123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
    defer-data-source-initialization: true
  
  application:
    name: PreAccountingSystem
  
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

jwt:
  secret: "bXktc2VjcmV0LWtleS1mb3Itand0LXNlY3VyaXR5LWVuY29kaW5nLWFuZC1kZWNvZGluZy1wYXNzLWFsbC1jb3Vac2U="
  expiration: 86400000

server:
  port: 8081
  servlet:
    context-path: /
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
  tomcat:
    threads:
      max: 200
      min-spare: 10

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true

file:
  upload:
    dir: /app/uploads/receipts

currency:
  api:
    key: ""
    base-url: "https://v6.exchangerate-api.com/v6"

logging:
  level:
    root: INFO
    preaccountingsystem: INFO
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# OpenAI API Configuration
openai:
  api-key: ${OPENAI_API_KEY:}
